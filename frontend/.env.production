# Production Environment Variables
# This file is automatically loaded by Vite during build for production

# API Base URL - Will be set by Render environment variable
# Using relative path so it works on the same domain
VITE_API_BASE_URL=/api

# Application environment
VITE_APP_ENV=production

# Logging level
VITE_LOG_LEVEL=error
