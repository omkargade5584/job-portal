# 1. Copy the pom.xml from your backend folder
COPY backend/pom.xml .

# 2. Let it download the dependencies
RUN mvn dependency:go-offline

# 3. FIX: Tell Docker to look inside the backend folder for your source code!
COPY backend/src ./src

# 4. Build the app
RUN mvn clean package -DskipTests